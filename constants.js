/**
 * API constants.
 * 
 * @returns An organized constants structure.
 */
function Constants() {
    return {
        error_messages: {
            AREA_404: "Area %s not found."
        }
    };
}

module.exports = Constants();